<h2>new</h2>

<%= simple_form_for[@cocktail] do |f| %>
  <div class="form-group">
    <%= f.input :name %>
  </div>

  <%= f.submit class: 'btn btn-primary' %>
<% end %>

<!-- collection: tag.all -->
